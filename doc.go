// Package feedrank implements FluxRank, Fluxa's transparent feed-ranking
// algorithm. Pure and stdlib-only: no I/O, no clock, seeded randomness only,
// so identical inputs always produce identical feeds.
// See ALGORITHM.md for the full specification.
package feedrank
